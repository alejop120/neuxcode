<%= form_with(model: course_has_subject, local: true) do |form| %>
  <% if course_has_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(course_has_subject.errors.count, "error") %> prohibited this course_has_subject from being saved:</h2>

      <ul>
      <% course_has_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :course_id %>
    <%= form.text_field :course_id %>
  </div>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id %>
  </div>

  <div class="field">
    <%= form.label :teacher_id %>
    <%= form.text_field :teacher_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
